import "./style.scss";
import { initApp } from "./app";

/* // This ensures that the dom content has loaded before running the function
document.addEventListener("DOMContentLoaded", initApp); */
initApp();
